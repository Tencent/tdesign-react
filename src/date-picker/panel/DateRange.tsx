// import React from 'react';
// import classNames from 'classnames';
import { StyledProps } from '../../_type';
import { TdDatePickerProps } from '../../_type/components/date-picker';

export interface DateRangePanelProps extends TdDatePickerProps, StyledProps {}

const DateRangePanel = () => {
  return null;
}

DateRangePanel.displayName = 'DateRangePanel';

export default DateRangePanel;
